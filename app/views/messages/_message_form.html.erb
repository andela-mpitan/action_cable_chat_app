<div class="message-input">
  <%= form_for(@message, remote: true) do |f| %>
    <%= f.text_area :content, placeholder: "Enter your message and press Enter to send" %>
    <%= f.submit "Send" %>
  <% end %>
</div>
